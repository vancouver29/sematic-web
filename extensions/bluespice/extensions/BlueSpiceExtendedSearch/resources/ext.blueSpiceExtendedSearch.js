(function( mw, $, bs, d, undefined ){

	function _query( term, params ) {
		var $dfd = $.Deferred();

		return $dfd;
	}

	bs.extendedSearch = {
		query: _query
	};
})( mediaWiki, jQuery, blueSpice, document );